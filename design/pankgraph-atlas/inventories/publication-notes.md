# GitHub publication notes

The Git version preserves the accepted 66-screen atlas while sharing fonts, images, styles and runtime scripts across pages. Screens remain available offline.

One tutorial reference image was replaced by the synthetic functional-tool capture because the public original showed donor identifiers and donor-level trait bars. The replacement is explicitly captioned in the tutorial. The original image is excluded from this repository. Static page text and unverified aggregate source counts remain existing UI copy.

Unused bundled Markdown/API guides, machine-specific capture scripts, temporary QA edits, the large ZIP and original embedded-asset snapshots are excluded. Source citations use repository-relative paths. Verification in this directory applies to the Git version.
