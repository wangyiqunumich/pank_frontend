Source audit of local current checkouts on 2026-09-15. No production fault injection, model calls, or live authentication attempted.

The application has unbounded dynamic server semantic clarifications, provider/browser Error.message strings and framework validation details. The catalog covers fixed literals and dynamic contracts; it is not a claim that every possible future string was enumerated.

Current src/index.js routes /review/*, /hirn-literature, /debug and /igv to Unavailable; HIRN and standalone graph demo messages are labeled inactive/standalone. Legacy resultpage_new.js and historical API markdown are not current active page wiring.

Browser Basic authentication occurs before the frontend; demoAuth in the frontend is a no-op anonymous placeholder. Auth errors are HTTP response text / native browser prompt, not a designed in-app sign-in screen.

Structured server recovery takes precedence over frontend fallback recovery, with original question, diagnosis, up to two verified suggestions, revision box and Cancel/Retry/Apply controls.

No dedicated user-visible layout-fallback banner found: optimized/partial/fallback and worker_busy/worker_failed/deadline_exceeded are backend diagnostics while available fallback graph is preserved.

Functional explorer summary/donor errors are stored but not given a dedicated inline error alert in current JSX; chart errors render. The GWAS validator currently uses raw query parameters rejected by the typed adapter and silently marks invalid. These are current behavior gaps for design review.

Initial intermediate record selection currently conflates empty result and request failure via schema-driven No QTL/GWAS data found error card. Treat this as current copy needing design review, not verified absence of source data.

Native browser image/download failures and standard MUI Autocomplete No options/browser validation labels are platform/library copy. Result functional plot has explicit image onError recovery; some legacy empirical images only test URL presence.
